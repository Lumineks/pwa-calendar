# Copy to .env (gitignored) and fill in the deployed Cloudflare Worker URL.
# Used by src/data/api.ts at build time as the base URL for all API calls.
VITE_WORKER_URL=https://your-worker.your-account.workers.dev
